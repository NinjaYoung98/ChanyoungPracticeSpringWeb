spring:
  datasource:
    url: jdbc:mysql://localhost:3306/OrderPractice
    username: root
    password: 19980903
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace